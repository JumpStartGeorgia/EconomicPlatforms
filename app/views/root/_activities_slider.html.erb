
  <div id="activityslider">

    <div class="top clear">
      <div class="left">
        <%= t('app.common.activities') %>
      </div>
      <div class="right">
        <div class="arrow disabled" data-direction="left">&#x25C4;</div>
        <% attr = (@activities.length > 1) ? '' : ' disabled' %>
        <div class="arrow<%= attr %>" data-direction="right">&#x25BA;</div>
      </div>
    </div>

    <div class="activities">

      <% @activities.each do |activity| %>
        <div class="each">
          <h3><%= link_to activity.title, activity_path(activity) %></h3>
          <div class="body clear">
            <div class="imagecontainer">
              <%= image_tag activity.images.last.file.url if !activity.images.last.nil? %>
            </div>
            <div class="text">
              <%= char_limit(activity.body, 120) %>
            </div>
          </div>
          <div class="showlink">
            <%= link_to t('app.common.view_full') + '...', activity_path(activity) %>
          </div>
        </div>
      <% end %>

    </div>

  </div>

