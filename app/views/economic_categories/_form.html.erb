<%= form_for @economic_category, :html => { :class => 'form-horizontal' } do |f| %>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                economic_categories_path, :class => 'btn' %>
  </div>
<% end %>
